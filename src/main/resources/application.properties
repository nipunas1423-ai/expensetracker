# ================= SERVER =================
server.port=${PORT:8080}

# ================= DATABASE CONFIG =================
# Use Render environment variables (recommended for deployment)
SPRING_DATASOURCE_URL = jdbc:postgresql://db.yvrrkgzuxpebwnvyjxiw.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=${postgres}
spring.datasource.password=${nipuvishu@1234}
spring.datasource.driverClassName=org.postgresql.Driver

# ================= JPA / HIBERNATE =================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# PostgreSQL dialect (optional in newer Spring Boot, but safe to keep)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ================= H2 CONSOLE =================
spring.h2.console.enabled=false
